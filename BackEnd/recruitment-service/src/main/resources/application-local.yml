spring:
  application:
    name: recruitment-service
  threads:
    virtual:
      # Kích hoạt Java 21 Virtual Threads cho toàn bộ Tomcat + Spring async
      # → Thread.sleep() trong polling LlamaParse không còn chặn platform thread
      # → Cho phép hàng trăm CV xử lý đồng thời mà không tốn thêm RAM
      enabled: true
  # Read .env
  config:
    import: optional:classpath:.env[.properties]
  # Database
  datasource:
    url: "${DB_URL}"
    username: "${DB_USERNAME}"
    password: "${DB_PASSWORD}"
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      maximum-pool-size: 20
      minimum-idle: 5
      connection-timeout: 30000
      idle-timeout: 600000
      max-lifetime: 1800000
      # Định kỳ 30s gửi ping SELECT 1 tới MySQL để giữ socket không bị server đóng
      # (Phòng lỗi "Stale Connection / Socket closed" khi polling LlamaParse 60-75s)
      keepalive-time: 30000
      # Test connection trước khi dùng - bắt connection chết trước khi văng lỗi runtime
      connection-test-query: SELECT 1
      validation-timeout: 5000
  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        jdbc:
          time_zone: Asia/Ho_Chi_Minh
    generate-ddl: true
    defer-datasource-initialization: true

  # RabbitMQ
  rabbitmq:
    host: localhost
    port: "${RABBITMQ_PORT}"
    username: "${RABBITMQ_USERNAME}"
    password: "${RABBITMQ_PASSWORD}"

  # Spring Mail — SMTP config cho email phỏng vấn
  mail:
    host: ${MAIL_HOST:smtp.gmail.com}
    port: ${MAIL_PORT:587}
    username: ${MAIL_USERNAME}
    password: ${MAIL_PASSWORD}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true


# CloudFlare
r2:
  account-id: ${R2_ACCOUNT_ID}
  access-key: ${R2_ACCESS_KEY}
  secret-key: ${R2_SECRET_KEY}
  bucket-name: ${R2_BUCKET_NAME}
  region: auto
  public-url: https://${R2_ACCOUNT_ID}.r2.cloudflarestorage.com/${R2_BUCKET_NAME}

# Local storage
storage:
  local:
    base-path: uploads

# LLamaParse
llama-parse:
  api-key: ${LLAMAPARSE_API_KEY}

# Gemini AI API
gemini:
  api-key: ${GEMINI_API_KEY}

# Batch
analysis:
  batch-size: 20

# GG Drive
google:
  drive:
    folder-id: ${FOLDER_ID}
    use-shared-drive: false

server:
  address: 0.0.0.0
  port: 8082

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always

# Chatbot internal secret — header X-Internal-Service phải khớp giá trị này
chatbot:
  internal-secret: ${CHATBOT_INTERNAL_SECRET:chatbot-service}
  from-email: ${MAIL_USERNAME}
  service-url: ${CHATBOT_SERVICE_URL:http://localhost:8085}
